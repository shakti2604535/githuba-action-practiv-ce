name: call build
on:
  push:
    branches:
         - main

jobs:
  build:
    environment: multi
    uses: ./reuseable.yml
    with:
      app_name: "my-web-app"
      environment: "production"
    secrets:
      docker_token: ${{ secrets.DOCKER_TOKEN }}